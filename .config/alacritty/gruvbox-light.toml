[colors.primary]
background = "#fbf1c7" # gruvbox-light(bg)
foreground = "#3c3836" # gruvbox-light(fg)

[colors.normal]
black   = "#fbf1c7" # gruvbox-light(bg)
red     = "#cc241d" # gruvbox-light(red)
green   = "#98971a" # gruvbox-light(green)
yellow  = "#d79921" # gruvbox-light(yellow)
blue    = "#458588" # gruvbox-light(blue)
magenta = "#b16286" # gruvbox-light(purple)
cyan    = "#689d6a" # gruvbox-light(aqua)
white   = "#7c6f64" # gruvbox-light(gray)

[colors.bright]
black   = "#928374" # gruvbox-light(gray)
red     = "#9d0006" # gruvbox-light(red)
green   = "#79740e" # gruvbox-light(green)
yellow  = "#b57614" # gruvbox-light(yellow)
blue    = "#076678" # gruvbox-light(blue)
magenta = "#8f3f71" # gruvbox-light(purple)
cyan    = "#427b58" # gruvbox-light(aqua)
white   = "#3c3836" # gruvbox-light(fg)
